let getters = {
    projects: state => {
        return state.projects
    },
    cycles: state => {
        return state.cycles
    },
    municipals: state => {
        return state.municipals
    }
}

export default getters